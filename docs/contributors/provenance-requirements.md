# Provenance requirements
Every document retains its source and retrieval location. Every claim, event and relationship must resolve to a document-owned evidence locator and exact quote. Synthetic records must be marked. Never silently combine publishers or infer effective dates.
