# Registry format
Registries use schema `1.0` and are validated with `policygraph validate-registry FILE`. IDs are lowercase, hyphenated and stable. Record the canonical HTTPS URL, publisher, source kind, topics, policy status and the date on which that status was verified.
