# Publishing a plugin
Register an entry point in group `policygraph.source_adapters`, pin a compatible PolicyGraph range, publish versioned Python artifacts, and document ownership. Signing is optional and should be claimed only when the publisher has a verified signing process. `policygraph adapters list` reads metadata only; loading is explicit.
